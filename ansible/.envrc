source_up

export ANSIBLE_CONFIG="${PWD}/ansible.cfg"
export ANSIBLE_INVENTORY="${PWD}/inventory/hosts.yml"
# SOPS_AGE_KEY_FILE inherited from root; community.sops plugin uses it
# to decrypt group_vars/*/secrets.sops.yml at play time
