# Managed by Ansible. certbot-dns-cloudflare credentials file — API token
# scoped to Zone:DNS:Edit for the bcochofel.com zone, used for the ACME
# DNS-01 challenge (writes/deletes the _acme-challenge TXT record).
dns_cloudflare_api_token = {{ cloudflare_api_token }}
