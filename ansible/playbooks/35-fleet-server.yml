---
- name: Fleet Server
  hosts: kibana
  gather_facts: true
  roles:
    - role: fleet_bootstrap
      when: fleet_server_enabled | default(false) | bool
    - role: fleet_server
      when: fleet_server_enabled | default(false) | bool
